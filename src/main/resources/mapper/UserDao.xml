<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group8.dao.UserDao">

    <select id="findAllUser" resultType="EtmsUser">
        select * from etms_user
    </select>

    <select id="findUserById" resultType="EtmsUser" parameterType="int">
        select * from etms_user where user_id = #{id}
    </select>

    <update id="updateUser" parameterType="EtmsUser">
        update etms_user set user_name = #{userName},
        user_dept = #{userDept},user_position = #{userPosition},user_telephone = #{userTelephone},
        user_email = #{userEmail},user_manager = #{userManager},user_role = #{userRole},
        user_company = #{userCompany},user_head_img = #{userHeadImg},updated_by = #{updatedBy},
        updated_time = #{updatedTime} where user_id = #{userId}
    </update>

    <update id="updatePassword">
        update etms_user set user_password = #{newPassword} where user_id = #{id}
    </update>
</mapper>
