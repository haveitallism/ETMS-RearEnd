<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group8.dao.EtmsOutlineDao">


    <select id="findAllCatalog" resultType="String">
        select distinct catalog from etms_outline where item_id = #{itemid}
    </select>

    <select id="findItemFile" resultType="String">
        select * from
    </select>
</mapper>